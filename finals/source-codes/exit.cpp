#include <bits/stdc++.h>
using namespace std;

int main()
{
        long long a, b;
        long long x, y;
        cin >> a >> b;
        cin >> x >> y;
            
        cout << abs(x-a) + abs(y-b) << endl;
    return 0;
}